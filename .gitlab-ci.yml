variables:
  REGISTRY: "registry-vpc.cn-shenzhen.aliyuncs.com/siss"

services:
  - docker:dind

before_script:
  - docker info

stages:
  - gitbook

build_image:
  stage: gitbook
  image: docker:stable-dind
  only:
    - development
    - ops

  script:
    - pwd 
    - ls -al
  tags:
    - docker